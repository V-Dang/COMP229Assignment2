//new file created for database
//npm install mongoose --save

//Must decalre URI, uses book_store database from local
module.exports = {
    URI: "mongodb://127.0.0.1:27017/book_store"         //numbers found on listening terminal mongod
};
